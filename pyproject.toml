[project]
name = "dataguard-ai"
version = "0.1.0"
description = "Telecom revenue-impact monitoring MVP for data pipelines"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
